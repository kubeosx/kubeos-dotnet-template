{
  "runtime": "Net70",
  "defaultVariables": "configuration=Debug",
  "documentGenerator": {
    "aspNetCoreToOpenApi": {
      "project": "${{values.project_name}}/${{values.project_name}}.csproj",
      "configuration": "$(configuration)",
      "useDocumentProvider": true,
      "documentName": "openapi",
      "output": "openapi.json"
    }
  }
}
